file(GLOB FUNCTIONS_SRCS ${CMAKE_CURRENT_SOURCE_DIR}/*.mex)
foreach(FUNCTION_SRC ${FUNCTIONS_SRCS})
    matlab_add_mex(NAME ${FUNCTIONS_SRC} SRC ${FUNCTION_SRC}.mex LINK_TO ${Matlab_LIBRARIES} moordyn)
endforeach()
